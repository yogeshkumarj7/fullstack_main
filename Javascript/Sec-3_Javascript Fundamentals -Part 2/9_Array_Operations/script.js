let favNum = [1, 2, 3, 4, 5, 6, 7, 8, 9];

// favNum.push(10);//add elements at last

// favNum.shift(111);//Add element at start

// favNum.unshift();//Removes first element
favNum.pop();
const popped = favNum.pop();
console.log(popped);
console.log(favNum);
