"use strict"; //By using this we can activate strict mode
let iamLazy = false;
let iamCool = true;

if (iamCool) iamLzy = false; //shows error if we use strict mode

if (iamLazy) {
  console.log("Oh sorry");
}

// we can't use reserved keywords as var name

let interface = "yogi"; // gives error
let private = 23; //same here
